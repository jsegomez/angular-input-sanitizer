import { InputControllerDirective } from './input-controller.directive';

describe('InputControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new InputControllerDirective();
    expect(directive).toBeTruthy();
  });
});
